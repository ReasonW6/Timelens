# Wayfinder 迁移与证据保留清单

迁移日期：2026-08-23
来源：`.scratch/timelens-v1/`
权威入口：[Timelens V1 决策地图](map.md)

## 已迁入 Git

- 完整决策地图和 15 张已解决票据，包括依赖、验收条件和解决记录。
- 5 份研究、2 份合同、交互原型及原型说明。
- Slint、Tauri、WinUI 对比 Spike 的源码、脚本、锁文件、测量合同和聚合结果。
- 窗口分类器校准源码、支持矩阵以及原始证据的历史字节数和 SHA-256。
- 性能验证源码、最终汇总、必要的进程采样/套件指标和安装体积代理数据。
- 里程碑 2 的正式 schema 容量生成器、隐私/性能脚本、三轮进程采样及精简运行闭环总表。
- WPR 运行状态、ETL 字节数和 SHA-256，以及安装器体积、签名状态和安装核验结果。

## 未迁入 Git，可安全重新生成

- Rust `target`、独立 build/release target、WinUI `bin`/`obj`/publish、便携 .NET SDK 和下载缓存。
- IExpress 研究安装器、展开载荷、安装验证副本和测试数据库/WebP。
- 单次运行目录、重复时间轴 JSON、调试日志及失败/被替代的性能运行。
- 三个 WPR ETL。现有机器缺少 WPA/WPAExporter，无法进一步提取可靠指标；状态、大小和哈希已保留。
- 窗口校准原始 JSONL。它们可能包含本机应用路径及窗口结构，只保留匿名化结论和历史哈希。
- 性能测试的合成 30 天 SQLite、逐事件 observer JSONL 和可确定性生成的数据。

这些本机产物不是正式实现的输入。需要复核时，应使用已保留源码和脚本在目标构建或发布候选上重新生成；不要从 `.scratch/` 恢复为产品代码。

## 2026-09-05 里程碑 3、4、5 证据

以下新增实现和证据随本次本地提交归档，尚未推送：

- `performance-validation/milestone-3-report.md`、`milestone-4-5-report.md`、工作记录与实机发布矩阵。
- 便携数据、AI、迁移恢复、安装维护与相应回归代码；合成负载、loopback AI、性能、截图、崩溃和隔离安装验收脚本。
- `performance-validation/evidence/milestone45-20260905/` 下的环境、容量、隐私、安装、崩溃、备份与截图结果、产物哈希、精简回归日志和进程组 CSV。
- 该证据目录中的 JPG 仅用于合成数据界面验收。实际桌面截图已从隔离数据集中清理，不进入正式证据。

`target/acceptance/84d2c6a9/` 中保留的合成数据库、导出包、详细安装日志与测试副本，以及 `target/tools/` 的编译器和 `dist/` 的安装包均可通过源码和脚本重新生成，不属于权威决策输入。需要删除时仍先按实际目标与正在运行的进程核对范围；本清单不构成对任意目录的递归清理授权。

## 迁移验收

- 地图、票据及 Markdown 相对链接必须在 Git 工作区内可达。
- 所有票据保持 `resolved`，地图没有未指定的阻塞决策。
- 被删除的大文件必须在相应报告或历史清单中留下用途、结论以及可用的字节数/哈希。
- 正式开发文档不得依赖 `.scratch/` 路径。
